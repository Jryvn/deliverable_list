using System.ComponentModel;

string[] menu = new string[] { 
    "eggs", "bacon", "spam", "sausage", "spam", "spam", "baked beans", "spam" };

foreach (string item in menu)
{
    Console.WriteLine(item);
}